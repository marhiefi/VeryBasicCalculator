## Basic one-line calculator exercise using JavaScript

### Includes checks for non-numeric or unselected user submissions (empty fields, special characters, letters, no operator selected) and user alerts.
